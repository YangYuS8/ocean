## 1. 主图输入与任务契约

- [x] 1.1 (MVP) 设计样本主图数据模型与读写接口，使每个 `sample` 只有一张当前主图。
- [x] 1.2 (MVP) 约定 YOLO 检测 job 的任务参数，明确其始终针对样本当前主图发起。
- [x] 1.3 (MVP) 定义自动检测建议的数据结构，至少包含摘要文本、是否有发现、类别计数与基础状态信息。

## 2. Laravel / Python 检测闭环

- [x] 2.1 (MVP) 在 Laravel 中支持手动创建 YOLO 检测 `analysis_job`，并限制同一主图只有一个进行中的检测任务。
- [x] 2.2 (MVP) 在 Python 侧接入 YOLO 模型执行，将结果转换为“摘要 + 轻量结构化建议”。
- [x] 2.3 (MVP) 完成 Laravel 与 Python 的结果回写闭环，使成功、失败和空建议都能稳定回到 `analysis_jobs` 语义中。
- [x] 2.4 (MVP) 评估并决定首版是否立即接入 Redis；若接入则保持最小异步链路，若不接入则保留后续演化边界。

## 3. 样本工作台交互

- [x] 3.1 (MVP) 在 `samples/:id` 增加主图区，支持上传/查看当前主图并引导用户先补齐主图后再运行检测。
- [x] 3.2 (MVP) 增加“自动检测建议”卡片，展示未运行、检测中、当前有效、已过期、失败等状态。
- [x] 3.3 (MVP) 在有旧建议时支持重新检测，并在刷新过程中保留旧建议可见直到新结果替换。
- [x] 3.4 (MVP) 保留原有 `analysis_jobs` 区块作为历史过程记录，避免建议消费层与过程记录层混淆。

## 4. 验证与后续演化

- [x] 4.1 (MVP) 覆盖关键场景验证：无主图、首次运行、有发现、无发现、失败重试、主图替换导致建议过期。
- [x] 4.2 (MVP) 验证当前 `docker compose` 启动路径、Laravel migration/seeder、Nuxt 页面和 Python 环境在本变更下仍能联通。
- [x] 4.3 (后续增值) 评估是否增加检测预览图、人工采纳记录以及将建议转化为候选结果的后续能力。
