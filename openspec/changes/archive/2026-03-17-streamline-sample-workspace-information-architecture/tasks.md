## 1. MVP - 样本列表信息重组

- [x] 1.1 盘点 `samples` 工作区现有列表字段、筛选条件和跳转路径，确认可直接复用的状态、结果、异常和分析任务信号
- [x] 1.2 调整样本列表项或表格列的信息优先级，优先呈现状态、风险和待办强度，并保留进入详情的清晰入口
- [x] 1.3 为样本列表补齐加载、空态和错误反馈下的信息架构表现，验证桌面端与移动端都能完成样本预判

## 2. MVP - 样本详情工作台重组

- [x] 2.1 重构 `samples/[id]` 页面顶部结构，按“概览 -> 风险阻塞 -> 推荐下一步”组织页面级引导层
- [x] 2.2 调整结果、异常和分析任务区块的排序与入口位置，确保页面级主动作与区块内动作语义一致
- [x] 2.3 统一结果待补录、异常未解决、分析任务待完成或失败时的提示文案与视觉层级，避免多个区块重复争抢主提示
- [x] 2.4 验证样本详情在桌面端和移动端的响应式顺序一致，避免移动端折叠后打乱信息优先级

## 3. MVP - 派生状态与回归验证

- [x] 3.1 基于现有接口数据实现列表与详情共用的前端派生判断逻辑，覆盖结果、异常和分析任务的关键状态组合
- [x] 3.2 为样本列表预判和详情页工作台引导补充组件测试或页面级验证用例，覆盖高风险、待办和空数据场景
- [x] 3.3 运行前端相关构建或测试命令，确认不需要新增 PHP API、Redis 队列联调或部署配置调整

## 4. 后续增值 - 工作区持续优化

- [x] 4.1 评估是否需要在后续版本引入可复用的样本工作区摘要模型或组合式函数，降低后续页面扩展成本
- [x] 4.2 收集列表预判与详情引导在真实使用中的误判场景，为后续是否增加后端 summary 接口提供依据
