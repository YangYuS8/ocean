## 1. 结果录入工作流接线

- [ ] 1.1 盘点现有样本详情页、样本结果接口与样本状态推进逻辑，确认结果录入所需字段、默认值和最小后端缺口。
- [ ] 1.2 在 `samples/:id` 详情页增加“新增结果”动作入口，并选择适合当前 Nuxt UI 工作台密度的承载形态（如 Modal、Drawer 或页内展开表单）。
- [ ] 1.3 接入结果创建请求，允许在样本详情页提交新结果并在成功后刷新结果列表与反馈信息。

## 2. MVP 结果录入表单

- [ ] 2.1 将第一版 `result_type` 收敛为 `salinity_test` 与 `ph_test` 两个固定选项。
- [ ] 2.2 为 `raw_value` 与 `normalized_value` 提供基于 `result_type` 的默认 JSON 模板，并支持恢复模板能力或等价的快速重置方式。
- [ ] 2.3 为 JSON 输入提供最小前端校验，仅校验合法 JSON 与对象结构，并向用户展示可理解的错误提示。
- [ ] 2.4 保持 `normalized_value`、`notes` 等字段可选，并为 `entered_by` 提供明确的 MVP 默认值口径。

## 3. 样本状态推进与接口收口

- [ ] 3.1 在结果创建成功后，以内聚业务规则将样本从 `registered` / `received` 推进到 `testing`，并保持已处于 `testing` 的样本状态不被过度推进。
- [ ] 3.2 确认结果记录仍以 `draft` 作为初始状态，不在本次实现完整复核流。
- [ ] 3.3 如结果录入工作流暴露必要缺口，仅补充最小范围的 Laravel 接口字段、默认值或状态推进逻辑改进。

## 4. 联调与验证

- [ ] 4.1 验证 `salinity_test` 与 `ph_test` 两类结果可在样本详情页成功录入并正确刷新结果列表。
- [ ] 4.2 验证结果录入后样本状态与首页摘要口径保持一致，且不破坏现有样本详情、结果只读展示和响应式布局。
- [ ] 4.3 验证错误 JSON、空态结果列表与提交失败反馈在桌面端和移动端都具有最小可用体验。
