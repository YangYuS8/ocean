## 新增需求

## 修改需求

### 需求:未解决异常必须参与页面级风险提示
系统必须将样本上的未解决异常整合进样本详情页的页面级风险或阻塞提示，而不是只在异常区块内部表达，以便用户进入页面时即可判断当前是否存在需要优先处理的问题；在新的样本工作区信息架构下，该风险提示还必须能够与列表页中的风险预判形成一致语义。

#### 场景:样本存在未解决异常
- **当** 当前样本存在 `open` 状态的异常
- **那么** 系统必须在样本详情页的页面级工作台提示中将其表达为当前风险或阻塞信息之一

## 移除需求
