## 1. 巡检任务工作区接线

- [x] 1.1 盘点现有 `inspection-tasks` 列表、详情、开始和提交接口字段，确认前端展示所需信息与最小缺口。
- [x] 1.2 将 `frontend/app/pages/inspections.vue` 从占位页升级为真实列表页，接入任务数据、状态筛选以及加载/空态/错误态。
- [x] 1.3 新增巡检任务详情页并接入任务详情接口，展示任务核心信息与状态。
- [x] 1.4 在巡检任务详情页接入 `start` / `submit` 动作，并在动作完成后刷新页面状态与用户反馈。

## 2. 样本工作区接线

- [x] 2.1 盘点现有 `samples` 列表、创建、详情与结果列表接口字段，确认样本工作区最小展示与创建表单所需信息。
- [x] 2.2 将 `frontend/app/pages/samples.vue` 从占位页升级为真实列表页，接入样本数据、基础筛选以及加载/空态/错误态。
- [x] 2.3 在样本工作区接入最小可用的新建样本表单，并在创建成功后给予反馈并跳转或刷新列表。
- [x] 2.4 新增样本详情页，展示样本基本信息、关联任务信息与结果区块。
- [x] 2.5 在样本详情页接入样本结果列表接口，并提供明确空态以作为后续结果录入和异常/分析动作的挂载基础。

## 3. 导航与体验收口

- [x] 3.1 校正首页、巡检任务与样本管理之间的跳转路径，确保核心工作区入口可达。
- [x] 3.2 为巡检任务与样本工作区统一处理操作成功、请求失败和空数据的提示体验。
- [x] 3.3 检查桌面端与移动端布局，避免新列表和详情页破坏当前工作台响应式结构。

## 4. 联调与验证

- [x] 4.1 验证首页摘要、巡检任务页与样本页在当前 Nuxt + Nginx + Laravel 编排下可正常联调。
- [x] 4.2 验证巡检任务状态推进、样本创建和样本详情/结果展示构成可演示的 MVP 闭环。
- [x] 4.3 如前端接线中暴露必要缺口，仅补充最小范围的 Laravel 接口字段或响应改进并完成回归验证。
