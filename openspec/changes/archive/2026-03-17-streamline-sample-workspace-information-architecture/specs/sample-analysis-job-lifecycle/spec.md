## 新增需求

## 修改需求

### 需求:分析任务状态必须参与页面级工作台引导
系统必须将分析任务状态整合进样本详情页的页面级工作台引导，使用户能够在进入页面时直接理解当前是否仍有分析过程待完成、是否存在失败任务需要优先处理，而不是仅在分析任务区块内部发现这些信息；分析任务区块中的局部提示必须服从页面级信息优先级，避免与主推荐动作重复竞争。

#### 场景:存在待完成的分析任务
- **当** 当前样本存在 `queued` 或 `running` 状态的分析任务
- **那么** 系统必须在样本详情页的页面级工作台引导中提示当前仍有分析过程在进行中或待完成

#### 场景:存在失败的分析任务
- **当** 当前样本存在 `failed` 状态的分析任务
- **那么** 系统必须在样本详情页的页面级工作台引导中将其表达为需要优先关注的风险或后续处理提示之一

## 移除需求
