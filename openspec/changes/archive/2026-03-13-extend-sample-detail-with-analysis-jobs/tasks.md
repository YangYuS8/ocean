## 1. 样本详情分析任务区块接线

- [x] 1.1 盘点现有样本详情页、分析任务接口与任务字段，确认当前样本的分析任务列表、创建动作和最小前后端缺口。
- [x] 1.2 在 `samples/:id` 页面主内容区新增分析任务区块，并将其放置在异常区块之后。
- [x] 1.3 接入当前样本的分析任务列表查询，按 `sample_id` 过滤展示相关任务记录。

## 2. MVP 分析任务发起与状态查看

- [x] 2.1 在样本详情页提供“发起分析”入口，并实现适合当前页面密度的页内展开表单或等价交互。
- [x] 2.2 将第一版 `job_type` 收敛为 `quality_assessment` 与 `anomaly_scan` 两个固定选项。
- [x] 2.3 接入分析任务创建请求，固定以当前样本作为上下文，并在成功后刷新任务列表与反馈信息。
- [x] 2.4 在任务列表中展示最小状态信息，至少包含任务类型、状态、发起人和入队时间；若接口已返回摘要或错误信息，则按状态补充展示。

## 3. 体验收口

- [x] 3.1 为分析任务区块补充加载态、空态、错误态和成功提示，保持与结果/异常区块一致的交互语气。
- [x] 3.2 检查样本详情页在桌面端与移动端的区块排布，避免分析任务区块引入明显拥挤或跳动。
- [x] 3.3 明确 `queued_by` 默认值与可选参数字段的 MVP 口径，避免前端表单过早膨胀为复杂参数系统。

## 4. 联调与验证

- [x] 4.1 验证样本详情页可成功发起 `quality_assessment` 与 `anomaly_scan` 两类分析任务，并在列表中看到新记录。
- [x] 4.2 验证分析任务状态展示与首页摘要 `queued_analysis_jobs` 指标口径保持一致。
- [x] 4.3 验证分析任务区块的引入不会破坏现有结果录入、异常处理和样本详情响应式布局。
