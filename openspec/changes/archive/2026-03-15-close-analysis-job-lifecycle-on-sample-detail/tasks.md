## 1. 分析任务生命周期动作接线

- [x] 1.1 盘点现有分析任务接口、服务层与样本详情页分析区块，确认生命周期动作需要新增的 API 路径、请求校验和状态字段更新点。
- [x] 1.2 在 Laravel 路由、控制器与请求校验层中新增分析任务最小生命周期动作入口，覆盖开始、成功、失败与取消所需的受控调用。
- [x] 1.3 在分析任务服务层实现 `queued -> running`、`running -> succeeded`、`running -> failed`、`queued -> cancelled` 的状态校验与时间字段写入。

## 2. 失败后重新发起闭环

- [x] 2.1 为失败任务新增“重新发起分析”动作，约束仅允许基于 `failed` 任务创建新的 `queued` 记录，并保留原任务失败历史。
- [x] 2.2 明确重新发起时复用的最小上下文字段，至少包括 `sample_id`、`job_type` 与可复用参数载荷。
- [x] 2.3 确保分析任务列表与详情查询可无歧义展示原失败任务和新发起任务并存的记录。

## 3. 样本详情页分析任务工作流收口

- [x] 3.1 在 `samples/:id` 分析任务区块中补齐 `running`、`cancelled` 等状态展示，并与现有 `queued`、`succeeded`、`failed` 保持一致语气。
- [x] 3.2 在成功任务卡片中展示结果摘要和最小后续引导，明确可继续录入结果、记录异常或暂不处理。
- [x] 3.3 在失败任务卡片中展示失败原因和最小后续引导，明确可重新发起分析或记录异常。
- [x] 3.4 为失败任务补充页内“重新发起分析”交互，确保新任务创建后可刷新列表并给予清晰反馈。

## 4. 联调与验证

- [x] 4.1 验证分析任务可按约定完成开始、成功、失败与取消流转，并拒绝非法状态跳转。
- [x] 4.2 验证失败任务重新发起后，原任务保留失败状态，新任务以 `queued` 状态出现在当前样本的任务列表中。
- [x] 4.3 验证样本详情页在桌面端与移动端下，分析任务区块新增的状态与后续动作不会破坏现有结果录入、异常处理和导航布局。
