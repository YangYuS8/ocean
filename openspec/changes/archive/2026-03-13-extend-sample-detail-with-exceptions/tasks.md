## 1. 样本详情异常区块接线

- [x] 1.1 盘点现有样本详情页、异常接口与异常字段，确认样本级异常列表、创建和解决动作所需的最小前后端缺口。
- [x] 1.2 在 `samples/:id` 页面主内容区新增异常处理区块，并将其放置在结果区块之后。
- [x] 1.3 接入当前样本的异常列表查询，按 `resource_type=sample` 与当前样本 ID 过滤展示异常记录。

## 2. MVP 异常录入与解决

- [x] 2.1 在样本详情页提供“记录异常”入口，并实现适合当前页面密度的页内展开表单或等价交互。
- [x] 2.2 将第一版异常 `category` 收敛为 `sample_quality`、`record_mismatch` 与 `handling_issue`，并沿用 `severity` 固定选项。
- [x] 2.3 接入异常创建请求，固定以当前样本作为资源，并在成功后刷新异常列表与反馈信息。
- [x] 2.4 对 `open` 状态异常提供最小“解决异常”动作，并在成功后刷新异常列表与反馈信息。

## 3. 状态与体验收口

- [x] 3.1 保持异常状态只影响异常本身，不因异常创建或解决自动改动样本状态。
- [x] 3.2 为异常区块补充加载态、空态、错误态和成功提示，保持与现有结果工作流一致的交互语气。
- [x] 3.3 检查样本详情页在桌面端与移动端的区块排布，避免异常区块引入明显拥挤或跳动。

## 4. 联调与验证

- [x] 4.1 验证样本详情页可成功记录 `sample` 级异常并在列表中看到新记录。
- [x] 4.2 验证 `open` 异常可在样本详情页被解决为 `resolved`，且列表状态正确刷新。
- [x] 4.3 验证异常创建/解决不会错误改变样本状态，并保持首页摘要口径与现有工作流一致。
